scale: 4
color_jitter_prob: 0.0
gray_prob: 0.0

# the first degradation process
resize_prob: [0, 0, 1]   # 強制 keep，不做隨機 up/down
resize_range: [1, 1]     # 固定 1，確保只做 scale=4 的 bicubic
gaussian_noise_prob: 0.0
poisson_scale_range: [0, 0]
gray_noise_prob: 0.0
jpeg_range: [100, 100]   # 不做 jpeg 壓縮

# the second degradation process
second_blur_prob: 0.0
resize_prob2: [0, 0, 1]  # 同樣強制 keep
resize_range2: [1, 1]
gaussian_noise_prob2: 0.0
poisson_scale_range2: [0, 0]
gray_noise_prob2: 0.0
jpeg_range2: [100, 100]

kernel_info:
    blur_kernel_size: 0        # 不用 kernel blur
    kernel_list: []            # 空
    kernel_prob: []            # 空
    sinc_prob: 0.0
    blur_sigma: [0, 0]
    betag_range: [0, 0]
    betap_range: [0, 0]

    blur_kernel_size2: 0
    kernel_list2: []
    kernel_prob2: []
    sinc_prob2: 0.0
    blur_sigma2: [0, 0]
    betag_range2: [0, 0]
    betap_range2: [0, 0]

    final_sinc_prob: 0.0
